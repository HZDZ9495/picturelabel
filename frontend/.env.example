# In development, set this to the backend API URL
VITE_API_URL=http://localhost:3000/api/annotate

# In production, if serving from the same server, you can leave it blank or set it as a relative path
# VITE_API_URL=/api/annotate
